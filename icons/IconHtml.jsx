import { FC } from 'react';

const IconHtml = ({ className }) => (
    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/html5/html5-original.svg" alt="HTML" className={className} />
);

export default IconHtml;